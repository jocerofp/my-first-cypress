// ================================== SETUP =========================================

// Init npm repo:
`npm init`

// Install cypress
`npm i cypress -D`

// Run cypress the first time, so the project is initialized
`npx cypress open`

// Run cypress in headless mode
`npx cypress run`